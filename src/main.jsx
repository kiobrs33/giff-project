import React from "react";
import ReactDOM from "react-dom/client";
import { GiffApp } from "./screens/GiffApp";
import "./assets/css/main.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <GiffApp />
  </React.StrictMode>
);
